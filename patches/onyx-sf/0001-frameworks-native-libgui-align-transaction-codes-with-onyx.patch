diff --git a/libs/gui/aidl/android/gui/ISurfaceComposer.aidl b/libs/gui/aidl/android/gui/ISurfaceComposer.aidl
index 8c19bbbba9..5674cdba7a 100644
--- a/libs/gui/aidl/android/gui/ISurfaceComposer.aidl
+++ b/libs/gui/aidl/android/gui/ISurfaceComposer.aidl
@@ -228,10 +228,6 @@ interface ISurfaceComposer {
      */
     void setGameContentType(IBinder display, boolean on);
 
-    /**
-     * Gets the maximum number of picture profiles supported by the display.
-     */
-    int getMaxLayerPictureProfiles(IBinder display);
 
     /**
      * Capture the specified screen. This requires READ_FRAME_BUFFER
@@ -611,4 +607,16 @@ interface ISurfaceComposer {
      * profiles.
      */
     oneway void setActivePictureListener(IActivePictureListener listener);
+
+    // MOVED TO THE END for the onyx-sf branch: AIDL assigns transaction
+    // codes by declaration order. This method does not exist in Onyx stock
+    // libgui, and declaring it mid-interface shifted the 45 methods after it
+    // by +1 -- our getCompositionPreference (34) then invoked their
+    // getDisplayedContentSamplingAttributes (33), which surfaced as
+    // SurfaceControl.getCompositionColorSpaces() == null killing system_server.
+    // Declared last, the 70 shared codes line up exactly.
+    /**
+     * Gets the maximum number of picture profiles supported by the display.
+     */
+    int getMaxLayerPictureProfiles(IBinder display);
 }
